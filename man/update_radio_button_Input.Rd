% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radio_button_input.R
\name{update_radio_button_Input}
\alias{update_radio_button_Input}
\title{Update a Govstyle radio button input on the client}
\usage{
update_radio_button_Input(
  session = shiny::getDefaultReactiveDomain(),
  inputId,
  label = NULL,
  choices = NULL,
  selected = NULL,
  inline = FALSE,
  small = FALSE,
  choiceNames = NULL,
  choiceValues = NULL
)
}
\arguments{
\item{session}{The \code{session} object passed to the Shiny server function.
Defaults to the current reactive domain.}

\item{inputId}{The id of the \code{\link[=radio_button_Input]{radio_button_Input()}} to update}

\item{label}{New label for the input, or \code{NULL} to leave unchanged}

\item{choices}{New vector of choices, or \code{NULL} to leave unchanged (if
elements of the vector are named then that name rather than the value is
displayed to the user)}

\item{selected}{The value to select, or \code{NULL} to leave unchanged}

\item{inline}{Whether the radios are inline. Only applies when \code{choices}
(or \code{choiceNames}/\code{choiceValues}) are also supplied, since regenerating the
options replaces the whole option container; if you do not re-pass it the
layout falls back to default. Defaults to FALSE}

\item{small}{Whether to use the smaller radios. Only applies when \code{choices}
(or \code{choiceNames}/\code{choiceValues}) are also supplied, since regenerating the
options replaces the whole option container; if you do not re-pass it the
layout falls back to default. Defaults to FALSE}

\item{choiceNames, choiceValues}{As in \code{\link[=radio_button_Input]{radio_button_Input()}}, an alternative
to \code{choices} allowing richer labels. Both must be supplied together}
}
\value{
Called for its side effect of sending a message to the client; no
return value
}
\description{
Server-side companion to \code{\link[=radio_button_Input]{radio_button_Input()}}, mirroring
\code{\link[shiny:updateRadioButtons]{shiny::updateRadioButtons()}}. Use it to change the selected option, the
available choices, or the label of an existing radio button group from
within an observer, for example to keep a cookies settings radio in sync
with a choice the user made elsewhere.
}
\details{
Only the arguments you supply are sent to the client; everything left at its
default of \code{NULL} is left untouched. Because it relies on
\code{session$sendInputMessage()}, the \code{inputId} is namespaced automatically when
called inside a Shiny module, so pass the unnamespaced id just as you would
to \code{\link[shiny:updateRadioButtons]{shiny::updateRadioButtons()}}.

This function is deliberately agnostic about why you are updating the radio,
which makes it a useful building block for packages that layer extra
behaviour (such as analytics cookie consent) on top of shinyGovstyle. See
the cookies and analytics vignette for an example of extending it.
}
\examples{
ui <- shiny::fluidPage(
  shinyjs::useShinyjs(),
  shinyGovstyle::radio_button_Input(
    inputId = "cookies",
    label = "Do you want to accept analytics cookies?",
    choices = c("Yes" = "yes", "No" = "no"),
    selected = "no",
    inline = TRUE
  ),
  shinyGovstyle::button_Input(inputId = "accept", label = "Accept cookies")
)

server <- function(input, output, session) {
  # Keep the radio in sync with a choice made elsewhere
  shiny::observeEvent(input$accept, {
    shinyGovstyle::update_radio_button_Input(
      session,
      inputId = "cookies",
      selected = "yes"
    )
  })
}
if (interactive()) shinyApp(ui = ui, server = server)
}
\seealso{
\code{\link[=radio_button_Input]{radio_button_Input()}}

Other Govstyle select inputs: 
\code{\link{button_Input}()},
\code{\link{checkbox_Input}()},
\code{\link{file_Input}()},
\code{\link{radio_button_Input}()},
\code{\link{select_Input}()}
}
\concept{Govstyle select inputs}
