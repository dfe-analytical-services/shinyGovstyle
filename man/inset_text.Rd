% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inset_text.R
\name{inset_text}
\alias{inset_text}
\title{Inset text}
\usage{
inset_text(inputId, content)
}
\arguments{
\item{inputId}{The id assigned to the component's root element. For Shiny
input components this is also the name used to access the value via
\verb{input$<inputId>}.}

\item{content}{Content to display in the inset. Accepts a plain character
string, or \code{shiny} tag objects such as \code{shiny::tags$b("Bold")} or a
\code{shiny::tagList()}.}
}
\value{
an inset text HTML shiny tag object
}
\description{
This function creates the GOV.UK
\href{https://design-system.service.gov.uk/components/inset-text/}{inset text}
component, used to differentiate a block of text from the content that
surrounds it, for example a quote, example, or additional information
about the page.
}
\examples{
ui <- shinyGovstyle::gov_page(
  shinyGovstyle::header(
    org_name = "Example",
    service_name = "User Examples",
    logo = "shinyGovstyle/images/moj_logo.png"
  ),
  shinyGovstyle::gov_main_layout(
    shinyGovstyle::gov_row(
      shinyGovstyle::gov_box(
        size = "two-thirds",
        shinyGovstyle::inset_text(
          inputId = "note",
          content = paste(
            "It can take up to 8 weeks to register a lasting power of",
            "attorney if there are no mistakes in the application."
          )
        ),
        shinyGovstyle::inset_text(
          inputId = "note-rich",
          content = shiny::tagList(
            shiny::tags$b("Important: "),
            "you can also pass tag objects."
          )
        )
      )
    )
  ),
  shinyGovstyle::footer(full = TRUE)
)

server <- function(input, output, session) {}
if (interactive()) shinyApp(ui = ui, server = server)
}
\seealso{
Other Govstyle feedback types:
\code{\link[=confirmation_panel]{confirmation_panel()}},
\code{\link[=details]{details()}},
\code{\link[=gov_summary]{gov_summary()}},
\code{\link[=gov_tag]{gov_tag()}},
\code{\link[=label_hint]{label_hint()}},
\code{\link[=noti_banner]{noti_banner()}},
\code{\link[=value_box]{value_box()}},
\code{\link[=warning_text]{warning_text()}}
}
\concept{Govstyle feedback types}
