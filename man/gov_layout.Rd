% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gov_layout.R
\name{gov_layout}
\alias{gov_layout}
\title{Page layout (deprecated)}
\usage{
gov_layout(..., inputID = "main", size = "full", width = "standard")
}
\arguments{
\item{...}{include the components of the UI that you want within the
main page.}

\item{inputID}{ID of the main div. Defaults to "main"}

\item{size}{Layout of the page. Optional are full, one-half, two-thirds,
one-third and one-quarter. Defaults to "full"}

\item{width}{Width of the component. One of \code{"standard"} (the default,
GOV.UK's usual 960px content width), \code{"three-quarters"}
(three-quarters of the viewport, never narrower than standard),
\code{"full"} (no max-width, so the container fills the viewport, with
grid gutters also removed), or a CSS length (e.g. \code{"1400px"},
\code{"90vw"}) for a custom max-width.}
}
\value{
a HTML shiny layout div
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}}

\code{gov_layout()} was deprecated in shinyGovstyle 0.3.0 and will be removed in
shinyGovstyle 1.0.0. Use \code{\link[=gov_main_layout]{gov_main_layout()}} with \code{\link[=gov_row]{gov_row()}} and
\code{\link[=gov_box]{gov_box()}} instead, which also gives the page its \verb{<main>} landmark:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Before
gov_layout(..., inputID = "main", size = "two-thirds", width = "standard")

# After
gov_main_layout(
  gov_row(gov_box(..., size = "two-thirds")),
  inputID = "main",
  width = "standard"
)
}\if{html}{\out{</div>}}

\code{inputID} moves to \code{gov_main_layout(inputID = )}, \code{size} to
\code{gov_box(size = )} and \code{width} to \code{gov_main_layout(width = )}. The inner
\verb{<inputID>_sub} element is not recreated, so wrap the content in
\code{shiny::tags$div(id = ...)} if your own CSS or JavaScript targets it. With
the default \code{size = "full"} you don't need \code{gov_row()} or \code{gov_box()} at
all: put the content straight into \code{gov_main_layout(...)}. Use one
\code{gov_main_layout()} per page; where \code{gov_layout()} was repeated inside tab
panels, put the content directly inside each panel instead (wrapped in
\code{gov_row(gov_box(..., size = ))} only if you set a narrower \code{size}).
}
\examples{
ui <- shinyGovstyle::gov_page(
  shinyGovstyle::header(
    org_name = "Example",
    service_name = "User Examples",
    logo = "shinyGovstyle/images/moj_logo.png"
  ),
  shinyGovstyle::gov_main_layout(
    shinyGovstyle::heading_text("Page heading", size = "l")
  ),
  shinyGovstyle::footer(full = TRUE)
)

server <- function(input, output, session) {}
if (interactive()) shinyApp(ui = ui, server = server)
}
\keyword{internal}
