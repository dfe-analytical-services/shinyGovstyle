% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/service_navigation.R
\name{service_navigation_server}
\alias{service_navigation_server}
\title{Auto-wire service navigation links to tabset panels}
\usage{
service_navigation_server(session, tabset_id, link_to_panel)
}
\arguments{
\item{session}{The Shiny session object}

\item{tabset_id}{The \code{id} of the \code{shiny::tabsetPanel()} or
\code{bslib::navset_hidden()} to switch}

\item{link_to_panel}{A character vector mapping nav link inputIds to
tab panel values. Pass a \strong{named} vector when inputIds differ from
panel values (names = inputIds, values = panel values). Pass an
\strong{unnamed} vector as shorthand for the 1:1 case — each element is
used as both the nav inputId and the panel value.}
}
\value{
NULL, called for side effects
}
\description{
Sets up one observer per nav link that switches the corresponding tab
panel when the link is clicked. Eliminates the per-link
\code{observeEvent()} boilerplate that multi-page apps would otherwise need.

Call once in your server function, after \code{\link[=service_navigation]{service_navigation()}} has been
added to the UI. The active nav highlight and (when
\code{auto_page_title = TRUE}) the browser tab title are already handled
client-side, so no additional wiring is needed.
}
\examples{
ui <- shiny::fluidPage(
  shinyGovstyle::service_navigation(
    c(sn_summary = "Summary", sn_detail = "Detail")
  ),
  bslib::navset_hidden(
    id = "tabs",
    bslib::nav_panel("summary", "Summary content"),
    bslib::nav_panel("detailed_data", "Detail content")
  )
)

server <- function(input, output, session) {
  # Nav inputIds differ from panel values
  shinyGovstyle::service_navigation_server(
    session,
    tabset_id = "tabs",
    link_to_panel = c(
      sn_summary = "summary",
      sn_detail = "detailed_data"
    )
  )

  # Or, when inputIds match panel values exactly:
  # shinyGovstyle::service_navigation_server(
  #   session,
  #   tabset_id = "tabs",
  #   link_to_panel = c("summary", "detailed_data")
  # )
}

if (interactive()) shiny::shinyApp(ui = ui, server = server)
}
\seealso{
Other Govstyle navigation: 
\code{\link{backlink_Input}()},
\code{\link{contents_link}()},
\code{\link{navigate_to}()},
\code{\link{service_navigation}()},
\code{\link{update_page_title}()},
\code{\link{update_service_navigation}()}
}
\concept{Govstyle navigation}
