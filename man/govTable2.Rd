% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/govTable2.R
\name{govTable2}
\alias{govTable2}
\title{Table Function}
\usage{
govTable2(
  inputId,
  df,
  caption,
  caption_size = "l",
  num_col = NULL,
  col_widths = list(),
  defaultPageSize = 10,
  pageSizeOptions = c(5, 10, 25, 50)
)
}
\arguments{
\item{inputId}{Input ID for the table.}

\item{df}{A dataframe to create a table.}

\item{caption}{Adds a caption to the table as a header.}

\item{caption_size}{Adjust the size of the caption. Options are "s", "m", "l", "xl" (default: "l").}

\item{num_col}{A vector of column names to format as numeric (right-aligned).}

\item{col_widths}{A named list specifying column widths using shorthand values (e.g., "one-quarter", "two-thirds").}

\item{defaultPageSize}{The default number of rows per page (default: 10).}

\item{pageSizeOptions}{A vector of available page size options (default: `c(5, 10, 25, 50)`).}
}
\value{
A `reactable` HTML widget object.
}
\description{
This function inserts a government-styled table using `reactable`.
}
\examples{
if (interactive()) {

  Months <- rep(c("January", "February", "March", "April", "May"), times = 2)
  Bikes <- c(85, 75, 165, 90, 80, 95, 85, 175, 100, 95)
  Cars <- c(95, 55, 125, 110, 70, 120, 60, 130, 115, 90)
  Trucks <- c(150, 130, 180, 160, 140, 175, 135, 185, 155, 145)
  Buses <- c(200, 180, 220, 210, 190, 215, 185, 225, 205, 195)
  example_data <- data.frame(Months, Bikes, Cars, Trucks, Buses)

  ui <- fluidPage(
    shinyGovstyle::header(
      main_text = "Example",
      secondary_text = "User Examples",
      logo="shinyGovstyle/images/moj_logo.png"),
    shinyGovstyle::banner(
      inputId = "banner", type = "beta", 'This is a new service'),
    shinyGovstyle::gov_layout(size = "two-thirds",
    govTable(
      "tab1", example_data, "Test Table", "l",
      num_col = c("Bikes", "Cars", "Trucks", "Buses"),
      col_widths = list(Months = "one-third"),
      defaultPageSize = 5, pageSizeOptions = c(5, 10, 20))
    ),

    shinyGovstyle::footer(full = TRUE)
  )

  server <- function(input, output, session) {}

  shinyApp(ui, server)
}
}
\keyword{table}
