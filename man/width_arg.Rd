% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{width_arg}
\alias{width_arg}
\title{Shared width argument (internal)}
\usage{
width_arg(width)
}
\arguments{
\item{width}{Width of the component. One of \code{"standard"} (the default,
GOV.UK's usual 960px content width), \code{"three-quarters"}
(three-quarters of the viewport, never narrower than standard),
\code{"full"} (no max-width, so the container fills the viewport, with
grid gutters also removed), or a CSS length (e.g. \code{"1400px"},
\code{"90vw"}) for a custom max-width.}
}
\description{
Documentation-only function. Holds the canonical \verb{@param} entry for a
component's own \code{width}, inherited via \verb{@inheritParams} by every
component that wraps a \code{.govuk-width-container}. \code{gov_page()} documents
its \code{width} separately, since there it sets an ambient default for child
components rather than the component's own width.
}
\keyword{internal}
